<main class="contenedor contenedor-formularios no-padding">
    <h1 class="my-5 titulo-web"><%= nombrePagina %></h1>

    <form class="default-form" method="POST">
        <div class="campo">
            <label>Nombre</label>
            <input class="form-control" type="text" value="<%= grupo.nombre %>" name="nombre" placeholder="Nombre Grupo">
        </div>

        <div class="campo descripcion">
            <label>Descripción</label>
            <div class="contenedor-editor">
                <input class="form-control" type="hidden" id="x" name="descripcion" value="<%= grupo.descripcion %>">
                <trix-editor input="x"></trix-editor>
            </div>
        </div>
        <div class="campo">
            <label>Categoría</label>
            <select class="form-control" name="categoriaId">
            <option value="" selected disabled>-- Elige una categoria --</option>
                <% categorias.forEach(categoria => {  %>
                    <option 
                        value="<%= categoria.id %>"
                        <%= (grupo.categoriaId === categoria.id ) ? 'selected' : '' %>
                    >
                    <%= categoria.nombre %></option>
                <% })  %>
            </select>
        </div>
        <div class="campo">
            <label>Sitio Web</label>
            <input class="form-control" type="url" name="url" placeholder="Sitio Web" value="<%= grupo.url %>">
        </div>
        <div class="campo enviar">
            <input type="submit" value="Guardar Cambios" class="btn btn-rosa">
        </div>
    </form>
</main>